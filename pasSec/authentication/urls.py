from django.urls import path, include
from . import views

urlpatterns = [
    # url('user/', views.UserRetrieveUpdateAPIView),
    # path('register', views.RegistrationAPIView, name='register'),
    # url('register/login/', views.LoginAPIView.as_view()),
]